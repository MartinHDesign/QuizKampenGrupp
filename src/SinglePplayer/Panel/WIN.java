package SinglePplayer.Panel;

import javax.swing.*;

public class WIN extends JPanel {
    private JLabel win = new JLabel("du vann");
    public WIN(){
        win.setVisible(true);
        add(win);
        setVisible(true);
    }
}
